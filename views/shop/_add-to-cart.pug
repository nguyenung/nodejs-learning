form(action='/cart' method="post" class="form-data") 
    input(type="hidden", name="_csrf", value=csrfToken)
    button(type="submit" class="button button-green") Add to Cart
    input(type="hidden" name="productId" value=`${product._id}`)
